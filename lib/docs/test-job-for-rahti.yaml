apiVersion: batch/v1
kind: Job
metadata:
  name: test-scraper-2
  namespace: alyakokeilut
spec:
  template:
    spec:
      containers:
        - name: scraper-trigger
          image: "curlimages/curl:latest"
          args:
            - /bin/sh
            - "-c"
            - 'curl -v -X POST -H "Authorization: Basic $(echo -n ${SCRAPER_USERNAME}:${SCRAPER_PASSWORD} | base64)" http://172.30.41.141:3000/api/cron'
          env:
            - name: SCRAPER_USERNAME
              value: "scraper-ari"
            - name: SCRAPER_PASSWORD
              value: "fCXItnkGg1CjctUA"
      restartPolicy: Never
